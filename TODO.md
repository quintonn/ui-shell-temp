# Next tasks

Update bootstrap code to inject the <div> element with id "root" if it doesn't exist. (if it doesn't already).

Make this an npm package that i can use (via a private npm repo in github).