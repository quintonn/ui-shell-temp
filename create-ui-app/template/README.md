# __PROJECT_TITLE__

Generated with `@quintonn/create-ui-app`.

## Scripts

- `npm run dev` starts the Parcel dev server.
- `npm run build` creates a production build in `dist`.
- `npm run preview` serves the app locally on port `4173`.
- `npm run typecheck` runs the TypeScript compiler without emitting files.
- `npm run lint` runs ESLint.

## Import Alias

The project exposes the `@/` alias for imports from `src`.

## Internal Dependency

This template expects `@quintonn/ui-app` to be available from your configured npm registry using the dependency spec chosen when scaffolding the project.